import React from "react";
import { View } from "react-native";

interface ComponentNameProps {}

const ComponenName = () => {
  return <View style={flex:1, backgroundColor: "cyan"} />;
};

export default ComponenName;